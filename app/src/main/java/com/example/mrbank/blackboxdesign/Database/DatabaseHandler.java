package com.example.mrbank.blackboxdesign.Database;import android.content.Context;import android.database.sqlite.SQLiteDatabase;import android.database.sqlite.SQLiteOpenHelper;/** * Created by mrbank on 19/05/16. */public class DatabaseHandler extends SQLiteOpenHelper {    //All Static Variables    //Database Version    private static final int DATABASE_VERSION = 22;    //Database Name    private static final String DATABASE_NAME= "blackboxDesign";    //Table Name    public static final String TABLE_LOCATION_DETAILS = "LocationDetails";    public static final String TABLE_LOCATION_REPORTS = "LocationReports";    //Table Columns Names    public static final String DETAILS_ID = "id";    public static final String DETAILS_NAME = "locationName";    public static final String DETAILS_LATITUDE = "latitude";    public static final String DETAILS_LONGTITUDE = "longtitude";    public static final String REPORTS_ID = "id";    public static final String REPORTS_START = "startTimeInMiliSeconds";    public static final String REPORTS_FINISH = "finishTimeInMiliSeconds";    public static final String REPORTS_DURATION = "durationInMiliSeconds";    public static final String REPORTS_LID = "locationId";    //Creating Tables    private static final String CREATE_DETAILS_TABLE = "CREATE TABLE " + TABLE_LOCATION_DETAILS+ "("            + DETAILS_ID + " INTEGER PRIMARY KEY AUTOINCREMENT," + DETAILS_NAME + " TEXT,"            + DETAILS_LATITUDE + " REAL," + DETAILS_LONGTITUDE + " REAL"            + ")";    //Creating Tables    private static final String CREATE_REPORTS_TABLE = "CREATE TABLE " + TABLE_LOCATION_REPORTS+ "("            + REPORTS_ID + " INTEGER PRIMARY KEY AUTOINCREMENT,"            + REPORTS_START + " INTEGER,"            + REPORTS_FINISH + " INTEGER,"            + REPORTS_DURATION + " INTEGER,"            + REPORTS_LID + " INTEGER,"            + "FOREIGN KEY("+REPORTS_LID +") REFERENCES "+TABLE_LOCATION_DETAILS+"("+DETAILS_ID+")"            + ")";    public DatabaseHandler(Context context) {        super(context, DATABASE_NAME, null, DATABASE_VERSION);    }    @Override    public void onCreate(SQLiteDatabase db) {        db.execSQL(CREATE_DETAILS_TABLE);        db.execSQL(CREATE_REPORTS_TABLE);    }    // Upgrading database    @Override    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {        // Drop older table if existed        db.execSQL("DROP TABLE IF EXISTS " + TABLE_LOCATION_DETAILS);        db.execSQL("DROP TABLE IF EXISTS " + TABLE_LOCATION_REPORTS);        // Create tables again        onCreate(db);    }}